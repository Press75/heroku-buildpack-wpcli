# WPCLI Heroku Buildpack

Adds WPCLI to the deployed Heroku app

# Requirements

This buildpack assumes you are deploying WordPress to a Heroku-ish environment.

# Use

Add the buildpack to your app.
